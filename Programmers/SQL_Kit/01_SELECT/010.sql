SELECT B.TITLE, R.BOARD_ID, R.REPLY_ID, R.WRITER_ID, R.CONTENTS, DATE_FORMAT(R.CREATED_DATE, '%Y-%m-%d') AS CREATED_DATE
FROM USED_GOODS_BOARD AS B LEFT OUTER JOIN USED_GOODS_REPLY AS R
ON B.BOARD_ID = R.BOARD_ID
WHERE B.CREATED_DATE LIKE '2022-10%' -- 2022년 10월에 '작성된'이므로 REPLY가 아닌 BOARD에서 조건 탐색
AND REPLY_ID IS NOT NULL
ORDER BY R.CREATED_DATE ASC, B.TITLE ASC;

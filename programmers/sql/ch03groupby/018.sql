SELECT SUM(G.SCORE) AS SCORE, G.EMP_NO, E.EMP_NAME, E.POSITION, E.EMAIL -- 함수적 종속성(functional dependency)
FROM HR_EMPLOYEES AS E, HR_GRADE AS G
WHERE E.EMP_NO = G.EMP_NO
GROUP BY G.EMP_NO
ORDER BY SCORE DESC
LIMIT 1;